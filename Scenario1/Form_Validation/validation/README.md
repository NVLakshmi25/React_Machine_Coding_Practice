If asked:

👉 How is validation handled?

Answer:

Form data stored in state

Separate errors state

validate() function checks each field

Regex for email & phone

Password match logic

Submit only if no errors

Controlled inputs everywhere


How do you validate checkboxes and reset them?

Say:

👉 “By storing selected values in state and binding the checked property to state, making them controlled inputs.”

How do you enforce password rules?

👉 “Using regex inside validation to check for required patterns like special characters, digits, and length.”